<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /hospital-management-system/public/
    
    # Handle query string style URLs
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?$1 [L,QSA]
</IfModule>